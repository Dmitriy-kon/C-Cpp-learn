#include <stdio.h>

// Пример функции
int main(void)
{
    int var, size;
    int total = 1024, buffer;

    size = var = 100;

    printf("var=%d\nsize=%d", var, size);
    printf("\ntotal=%d", total);
    printf("\nbuffer=%d", buffer);

    return 0;
}